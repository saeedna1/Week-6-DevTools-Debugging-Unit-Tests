//const { describe } = require("mocha");
/*this funcation are setting up from vedio week 6
function doSometing(x,y) {
  if (typeof x != 'string'){
    throw new Error('x must be a string');
  }
    return x + y;
}*/


//new this setting up 

describe('Test War Card Game', () => {
  it('Passes', () => {
    chai.expect(1).to.eql(1);
  });
  
  it('Fails', () => {
    chai.expect(1).to.eql(1);
  });
});

mocha.run();






